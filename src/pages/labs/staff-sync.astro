---
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import '../../styles/global.css';
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="StaffSync: An inventory management and staff scheduling app that replaces Excel sheets forever. Built with PWA, Supabase, and Tailwind." />
    <title>StaffSync - Digital8 Labs</title>
  </head>
  <body class="bg-[#0a0a0a] text-white min-h-screen antialiased">
    <!-- Navigation Bar -->
    <Navbar />

    <!-- Page Content -->
    <main class="min-h-screen">
      <!-- Cyber Engineering Grid Background -->
      <div class="fixed inset-0 cyber-grid pointer-events-none opacity-30"></div>
      
      <!-- Radial Gradient Spotlight Effect -->
      <div class="fixed inset-0 pointer-events-none" style="background: radial-gradient(circle at center, rgba(255, 255, 255, 0.02) 0%, rgba(0, 0, 0, 0.4) 50%, rgba(0, 0, 0, 0.8) 100%);"></div>

      <!-- Back Navigation -->
      <section class="pt-24 pb-8 px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-7xl mx-auto">
          <a
            href="/#labs"
            class="inline-flex items-center text-gray-400 hover:text-white transition-colors duration-200 text-sm group"
          >
            <span class="mr-2 group-hover:-translate-x-1 transition-transform">←</span>
            Back to Solutions
          </a>
        </div>
      </section>

      <!-- Project Hero -->
      <section class="py-12 px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-7xl mx-auto">
          <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
            <!-- Left: Title & Info -->
            <div class="space-y-6">
              <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold leading-tight text-white">
                StaffSync: Inventory & Scheduling App
              </h1>
              <p class="text-xl sm:text-2xl text-gray-400 leading-relaxed">
                Internal tool for inventory management and staff scheduling. Replaces Excel sheets forever.
              </p>
              
              <!-- Tags -->
              <div class="flex flex-wrap gap-3 pt-4">
                <span class="px-4 py-2 bg-white/5 border border-white/10 rounded-full text-sm text-white/80">PWA</span>
                <span class="px-4 py-2 bg-white/5 border border-white/10 rounded-full text-sm text-white/80">Supabase</span>
                <span class="px-4 py-2 bg-white/5 border border-white/10 rounded-full text-sm text-white/80">Tailwind</span>
              </div>
            </div>

            <!-- Right: Visual Card -->
            <div class="flex items-center justify-center">
              <div class="relative">
                <!-- Glow effect behind image -->
                <div class="absolute inset-0 bg-[#c9d40c]/20 rounded-2xl blur-2xl scale-110"></div>
                <!-- Image -->
                <img
                  src="/staffsync.webp"
                  alt="Warehouse staff syncing inventory on tablets"
                  class="relative w-full max-w-md h-[500px] rounded-2xl shadow-2xl border border-white/10 object-cover"
                />
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Features Grid -->
      <section class="py-16 px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-7xl mx-auto">
          <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
            <!-- Feature 1: Offline Mode -->
            <div class="bg-white/5 backdrop-blur-md border border-[#c9d40c]/20 rounded-xl p-8 shadow-2xl">
              <div class="flex items-center gap-3 mb-4">
                <div class="w-10 h-10 bg-[#c9d40c]/20 rounded-lg flex items-center justify-center">
                  <span class="text-2xl">📱</span>
                </div>
                <h2 class="text-xl font-bold text-white">Offline Mode</h2>
              </div>
              <p class="text-gray-400 leading-relaxed">
                Works without internet. Sync automatically when connection is restored. No lost data, no frustration.
              </p>
            </div>

            <!-- Feature 2: Real-time Sync -->
            <div class="bg-white/5 backdrop-blur-md border border-green-500/20 rounded-xl p-8 shadow-2xl">
              <div class="flex items-center gap-3 mb-4">
                <div class="w-10 h-10 bg-green-500/20 rounded-lg flex items-center justify-center">
                  <span class="text-2xl">🔄</span>
                </div>
                <h2 class="text-xl font-bold text-white">Real-time Sync</h2>
              </div>
              <p class="text-gray-400 leading-relaxed">
                Changes appear instantly across all devices. No manual refresh, no version conflicts. Team stays in sync.
              </p>
            </div>

            <!-- Feature 3: Excel Replacement -->
            <div class="bg-white/5 backdrop-blur-md border border-[#c9d40c]/20 rounded-xl p-8 shadow-2xl">
              <div class="flex items-center gap-3 mb-4">
                <div class="w-10 h-10 bg-[#c9d40c]/20 rounded-lg flex items-center justify-center">
                  <span class="text-2xl">🚫</span>
                </div>
                <h2 class="text-xl font-bold text-white">Excel Replacement</h2>
              </div>
              <p class="text-gray-400 leading-relaxed">
                Say goodbye to corrupted spreadsheets and version chaos. Built for teams, designed for mobile.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- The Problem vs Solution Grid -->
      <section class="py-16 px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-7xl mx-auto">
          <div class="grid md:grid-cols-2 gap-8 lg:gap-12">
            <!-- Left: The Problem -->
            <div class="bg-white/5 backdrop-blur-md border border-red-500/20 rounded-xl p-8 shadow-2xl">
              <div class="flex items-center gap-3 mb-4">
                <div class="w-10 h-10 bg-red-500/20 rounded-lg flex items-center justify-center">
                  <span class="text-2xl">⚠️</span>
                </div>
                <h2 class="text-2xl font-bold text-white">The Problem</h2>
              </div>
              <p class="text-gray-400 leading-relaxed text-lg">
                Excel sheets break. They don't sync. They corrupt. Your team wastes hours every week managing spreadsheets that were never designed for modern workflows.
              </p>
            </div>

            <!-- Right: The Solution -->
            <div class="bg-white/5 backdrop-blur-md border border-green-500/20 rounded-xl p-8 shadow-2xl">
              <div class="flex items-center gap-3 mb-4">
                <div class="w-10 h-10 bg-green-500/20 rounded-lg flex items-center justify-center">
                  <span class="text-2xl">⚡</span>
                </div>
                <h2 class="text-2xl font-bold text-white">Our Solution</h2>
              </div>
              <p class="text-gray-400 leading-relaxed text-lg">
                A custom-built Progressive Web App that works offline, syncs in real-time, and adapts to your specific business needs. No more Excel headaches.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Performance Metrics -->
      <section class="py-16 px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-7xl mx-auto">
          <div class="grid md:grid-cols-3 gap-8">
            <!-- Metric 1 -->
            <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-xl p-8 text-center shadow-2xl">
              <p class="text-gray-400 text-sm uppercase tracking-wider mb-3">Time Saved</p>
              <p class="text-4xl sm:text-5xl font-bold text-green-400 mb-2">10hrs</p>
              <p class="text-xs text-gray-500 mt-2">Per week on admin tasks.</p>
            </div>

            <!-- Metric 2 -->
            <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-xl p-8 text-center shadow-2xl">
              <p class="text-gray-400 text-sm uppercase tracking-wider mb-3">Offline Support</p>
              <p class="text-4xl sm:text-5xl font-bold text-[#c9d40c] mb-2">100%</p>
              <p class="text-xs text-gray-500 mt-2">Works perfectly in dead zones.</p>
            </div>

            <!-- Metric 3 -->
            <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-xl p-8 text-center shadow-2xl">
              <p class="text-gray-400 text-sm uppercase tracking-wider mb-3">Sync Speed</p>
              <p class="text-4xl sm:text-5xl font-bold text-[#c9d40c] mb-2">&lt;1s</p>
              <p class="text-xs text-gray-500 mt-2">Zero conflicts across teams.</p>
            </div>
          </div>
        </div>
      </section>

      <!-- Technical Deep Dive -->
      <section class="py-16 px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-4xl mx-auto">
          <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-xl p-8 shadow-2xl">
            <h2 class="text-2xl font-bold text-white mb-6">Technical Deep Dive</h2>
            
            <!-- Code Snippet Visual -->
            <div class="bg-[#1a1a1a] border border-white/10 rounded-lg p-6 font-mono text-sm overflow-x-auto">
              <div class="flex items-center gap-2 mb-4">
                <div class="w-3 h-3 bg-red-500 rounded-full"></div>
                <div class="w-3 h-3 bg-yellow-500 rounded-full"></div>
                <div class="w-3 h-3 bg-green-500 rounded-full"></div>
              </div>
              <pre class="text-gray-300"><code><span class="text-purple-400">async</span> <span class="text-blue-400">function</span> <span class="text-cyan-400">syncInventory</span>() 
  <span class="text-purple-400">const</span> queue = <span class="text-blue-400">await</span> <span class="text-cyan-400">getOfflineQueue</span>()
  
  <span class="text-gray-500">// Sync when online</span>
  <span class="text-purple-400">if</span> (navigator.onLine) 
    <span class="text-purple-400">await</span> <span class="text-cyan-400">supabase</span>
      .from(<span class="text-orange-400">'inventory'</span>)
      .upsert(queue)
</code></pre>
            </div>
            
            <p class="text-center text-gray-400 text-sm mt-6 italic">
              Clean code. No spaghetti.
            </p>
          </div>
        </div>
      </section>

      <!-- CTA Section -->
      <section class="py-20 px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="max-w-4xl mx-auto text-center">
          <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">
            Ready to replace Excel forever?
          </h2>
          <a
            href="mailto:daniel@digital8.ca?subject=I need a custom business app"
            class="inline-block px-10 py-5 bg-[#c9d40c] text-black font-bold text-lg rounded-lg hover:bg-[#c9d40c]/90 hover:scale-105 active:scale-95 transition-all duration-200 shadow-lg shadow-[#c9d40c]/30"
          >
            Get a Quote for Custom Apps
          </a>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <Footer />
  </body>
</html>
